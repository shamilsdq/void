#include "command.h"


int Command::build ()
{
    // read config file
    // build dll from project

    return 0;
}


int Command::serve ()
{
    // interface with project dll
    // execute on localhost port

    return 0;
}